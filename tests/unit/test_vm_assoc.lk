$list = [];
push($list, ["method" => "GET", "path" => "/"]);
push($list, ["method" => "POST", "path" => "/login"]);

route("GET", "/debug", function() use ($list) {
    foreach ($list as $item) {
        $m = $item["method"];
        $p = $item["path"];
        print($m . "|" . $p . "\n");
    }
});
