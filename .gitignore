# Build çıktıları
cpp/build*/
cpp/.look_cache/
cpp/build.bat
# Portatif build — kaynaktan statik OpenSSL cache'i (büyük, üretilir)
cpp/.ssl-static/

# Visual Studio
.vs/
*.slnx
*.vcxproj.user

# Gizli / credential
.env
.env.local
.env.production
.env.*
tools/deploy/env.production
tools/deploy/.env*
*.json.key
*firebase-adminsdk*.json
*-firebase-adminsdk-*.json
.look_history
.claude/

# Log ve debug dosyaları
logs/
*.log
dbg_*.txt
plink_*.txt
stress_*.txt
*_results.txt
*_debug.txt
*_stderr.txt
*_stdout.txt
dbg.txt

# Veritabanları
*.db
*.db-shm
*.db-wal

# Geçici test dosyaları (sadece proje kökü — tests/ alt dizinleri hariç)
/test_*.lk
/test_*.py
/test_*.sh

# Deploy / debug scriptleri
deploy_*.sh
check_*.sh
debug*.sh
stress*.sh
leak-monitor.sh
memory_72h.sh
concurrency-test.sh
run-tests*.sh
save_results.sh
ws_test.sh
xss_test.sh
mysql-recovery-test.sh
regen_nginx.sh
setup_chat_nginx*.sh
find_plesk_cmd.sh

# PowerShell geliştirme scriptleri
deploy*.ps1
fcgi_stress.ps1
write_index.ps1
stress_test.ps1

# Python yardım scriptleri
write_installer.py
run_pg_test.bat

# Binary / release dosyaları (bunlar GitHub Releases'e gider)
*.zip
*.tar.gz
*.exe
*.vsix

# Plesk extension binary'leri (GitHub Releases'e gider, repo'ya alınmaz)
platforms/plesk/bin/

# macOS
.DS_Store

# IDE build dizinleri
build/
build-almalinux8/

# Derlenmiş binary çıktıları (build'den üretilir, commit edilmez)
cpp/docker/bin_out/
cpp/docker/.look_cache/
platforms/plesk/htdocs/bin/
platforms/plesk/bin/

# curl test artifact'ları
test_cookies.txt
*_cookies.txt
# RPM build ciktisi (uretilir)
platforms/linux/rpm/out/
cpp/build-rpm/
node_modules/

# Internal dev instructions (private — not published)
CLAUDE.md

# Dil dokumantasyonu IZLENIR (docs/index.html = otoriter API referansi + conformance
# fixture kaynagi; docs/*.md = deployment/protokol). Neden: 12 dogfooding bulgusunun
# yarisi dokuman<->gerceklik drift'i (session::has, multipart, systemd www-data/lk...);
# conformance guard yalniz FONKSIYON VARLIGINI gorur, DAVRANIS/TALIMAT iddialarini degil.
# docs/ sürüm-disi kaldikca kod degisir dokuman ayri kalir, kimse diff'te yan yana gormez.
# AYRI web property'ler (pazarlama ana sayfasi + paket dizini) IGNORE kalir — bu repo
# "dil cekirdegi": alt siteler kendi yerinde.
docs/codlook.com/
docs/packages.codlook.com/

# --- Yayin hatti: TARIFI izle, CIKTIYI yok say ---
# Drift kok-nedeni: izlenmeyen plesk/bin gunlerce stale kaldi -> yamasiz zip uretildi.
# Surum kontrolu ilk git status'ta gorurdu. Build script + paket kaynagi IZLENIR;
# derlenmis ikili/zip/rpm/dist CIKTIDIR, yok sayilir.
# (NOT: gitignore satir-ici yorum desteklemez — yorumlar ayri satirda.)
# VSCode eklentisi: KAYNAK izlenir; cikti (vsix paketi, node_modules, gecici) yok sayilir:
platforms/vscode/node_modules/
platforms/vscode/_vsix_tmp/
platforms/vscode/*.vsix
platforms/vscode/*.map
# Paket ciktilari:
platforms/**/*.zip
platforms/**/*.rpm
platforms/**/*.exe
platforms/**/*.vsix
# Derlenmis ikili (drift buradan cikti) + frontend cikti:
platforms/plesk/bin/
platforms/plesk/htdocs/bin/
platforms/plesk/htdocs/dist/
# Yerel kurulum testi + junk (bozuk komuttan kalma bos dizin):
platforms/plesk/test/
platforms/plesk;C/
# cpp/docker: yalniz uretim tarifi izlensin; test infra + cikti disarda:
cpp/docker/*
!cpp/docker/Dockerfile.production
!cpp/docker/Dockerfile.fuzz
# Fuzz cikti (corpus fuzzer'in urettigi + crash tekrar-uretilebilir; seed'ler harness yorumunda)
cpp/tests/fuzz/corpus_*/
cpp/tests/fuzz/crash-*
cpp/tests/fuzz/leak-*
cpp/tests/fuzz/timeout-*

# Yerel calisma notlari (repoda tutulmuyor)
PROJE_DURUMU.md
DENETIM.md
