# Benchmark harnesses + hygiene rules (SPEC.md) are tracked — the durable output.
# Result outputs, deps and container artefacts are not.
node_modules/
logs/
*.log
*.csv
*.out
_*
mallocprobe/*.so
mallocprobe/lk-fcgi-old
mallocprobe/lk-fcgi-new
